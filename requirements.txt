# Core (Phase 1)
Django>=5.2,<5.3
mysqlclient>=2.2
python-dotenv>=1.0
openai>=2.0
elevenlabs>=2.0

# Added in later milestones:
# faster-whisper>=1.0   # local subtitles (milestone 10, optional)
#
# System binaries (not pip): ffmpeg + ffprobe on PATH for the render step.
